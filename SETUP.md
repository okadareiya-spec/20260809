# 疎通検証（V-1〜V-3）の手順

技術仕様書 12.1 の V-1〜V-3 を確認するための手順。**ここが通らなければ、GAS 単体という実装方式の前提が崩れる。** 本実装に入る前に必ず完了させること。

確認する内容は3点。

| # | 確認すること | 通らなかった場合 |
|---|---|---|
| V-1 | GAS ウェブアプリの `/exec` が返すリダイレクトを LINE が追従し、`doPost` が実際に呼ばれるか | **実装方式の再検討が必要**（Node.js + Sheets API へ変更） |
| V-2 | Webhook URL のクエリパラメータを `e.parameter` で受け取れるか | リクエスト検証の手段がなくなる。方式の再検討が必要 |
| V-3 | LINE Developers の「検証」ボタンが送る空の `events` で落ちないか | 検証が失敗表示になるが実配信は届く、という紛らわしい状態になる |

---

## 事前に用意するもの

1. **LINE Developers の Messaging API チャネル**
   - https://developers.line.biz/console/ で作成
   - 「Messaging API設定」から**チャネルアクセストークン（長期）**を発行しておく
   - 同じ画面で**応答メッセージをオフ、Webhook を利用するをオン**にする（自動応答が邪魔をするため）

2. **Google スプレッドシート1つ**
   - 名前は任意（例：`会議室予約`）
   - **これに紐づくスクリプトを使う**ので、独立した Apps Script プロジェクトを作らないこと

---

## 手順

### 1. スプレッドシートから Apps Script を開く

スプレッドシートのメニューで **拡張機能 > Apps Script**。これで**コンテナバインドスクリプト**が作られる。

開いたら、**プロジェクトの設定**（左の歯車）から **スクリプトID** を控える。

### 2. ローカルと同期する

```bash
clasp login
```

ブラウザが開くので、スプレッドシートを作った Google アカウントで承認する。

```bash
cd C:\Users\surij\Documents\line-reservation-bot\src
clasp clone <スクリプトID>
```

`clasp clone` は既存ファイルを取ってくるので、**このリポジトリの `main.js` と `appsscript.json` で上書きしてから** push すること。

```bash
clasp push
```

### 3. トークンを保存する

Apps Script エディタで `main.js` を開き、`setUp` 関数の `channelAccessToken` に、手順の事前準備で発行したチャネルアクセストークンを貼る。

エディタ上部の関数選択で **`setUp`** を選び、**実行**。初回は権限の承認を求められるので許可する。

**実行が終わったら、貼り付けたトークンをコードから消すこと。** 値はスクリプトプロパティに保存されているので、コードに残す必要はない。

### 4. ウェブアプリとしてデプロイする

**デプロイ > 新しいデプロイ > 種類の選択でウェブアプリ**を選び、以下を設定する。

| 項目 | 値 |
|---|---|
| 次のユーザーとして実行 | **自分** |
| アクセスできるユーザー | **全員** |

「アクセスできるユーザー」を「全員」にしないと、LINE からの匿名 POST を受け取れない。

### 5. Webhook URL を設定する

エディタで **`showWebhookUrl`** を実行し、ログに出た URL をコピーする。形式は以下になる。

```
https://script.google.com/macros/s/〈デプロイID〉/exec?token=〈UUID〉
```

LINE Developers コンソールの **Messaging API設定 > Webhook URL** に貼り、**Webhook の利用をオン**にする。

### 6. 検証する

**まず「検証」ボタンを押す。** これが V-3 の確認になる。

> **成功しなくても、ここで諦めないこと。** GAS はリダイレクトを返すため、検証ボタンがエラー表示になっても実配信は届く場合がある。次の手順に進んで実機で確認する。

次に、**チャネルのQRコードから LINE で友だち追加し、何かメッセージを送る。**

「受信しました: 〈送った文字〉」が返ってくれば、**V-1・V-2・V-3 のすべてが通ったことになる。**

### 7. ログを確認する

Apps Script エディタの **実行数**（左メニュー）から `doPost` の実行ログを開く。以下が出ていることを確認する。

```
[V-2] e.parameter = {"token":"..."}
[V-1] doPost に到達しました。
[V-3] events 件数 = 1
reply 応答 = 200 {}
```

**`[V-2]` にトークンが入っていることが重要。** ここが `{}` や `null` なら、クエリパラメータが受け取れていないということであり、リクエスト検証の手段がなくなる。

---

## うまくいかないときの切り分け

| 症状 | 確認すること |
|---|---|
| 何も返ってこない。ログにも実行が出ない | Webhook URL が正しいか。「Webhook の利用」がオンか。デプロイのアクセス設定が「全員」か |
| ログに `[V-2] トークンが一致しません` | URL の `?token=` の値が `showWebhookUrl` の出力と一致しているか |
| ログは出るが返信が来ない | `reply 応答` の中身を見る。401 ならチャネルアクセストークンが誤っている |
| 自動で定型文が返ってくる | LINE 側の**応答メッセージ**がオンになっている。オフにする |
| コードを直したのに反映されない | `clasp push` の後、**デプロイを更新**する必要がある（新しいバージョンとして保存） |

---

## この検証が通った後

`src/main.js` を破棄し、3文書の仕様に沿った本実装に着手する。読む順序は `docs/README.md` を参照。
